# Everything needed to run the desktop app.
# The adbtouch library alone needs nothing beyond the standard library:
#   pip install -e .
customtkinter>=5.2,<6
opencv-python>=4.5
numpy>=1.21
Pillow>=9.0
